import { PlanService } from './plan.service';
import { Module } from '@nestjs/common';

@Module({
    imports: [],
    controllers: [],
    providers: [
        PlanService,],
})
export class PlanModule { }
